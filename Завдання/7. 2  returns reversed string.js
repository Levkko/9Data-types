// A function which returns reversed string

function reverse(str) { 
    let letters = str.split('');
    letters.reverse();
    return letters.join('');
 }
reverse('abc') // 'cba'

//split,reverse,join - я сам вивчив з інтернету
